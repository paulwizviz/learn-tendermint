version: '3.8' # this is obsolete and optional

services:

  solo:
    build:
      context: ../
      dockerfile: ./build/solo.dockerfile
      args:
        OS_VER: 3.18
        GO_VER: 1.22.1
        TMINT_VER: v0.35.9
    image: ${TENDERMINT_NODE_IMAGE}
